# MyCargo UI Generator - Environment Variables
# Copy this file to .env.local and fill in your values

# Optional: Vercel KV for export feature (auto-configured on Vercel)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Optional: Base URL for exports (auto-detected on Vercel)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Note: API keys (OpenAI/Anthropic) are stored in browser localStorage
# No server-side API keys are required
